# Base URL of the admin shim (the Rust BE). Used when VITE_USE_MOCK is not "true".
VITE_ADMIN_API=http://localhost:8787

# Run against a local in-memory mock API (no backend needed) — set to "true" to demo standalone.
VITE_USE_MOCK=true
